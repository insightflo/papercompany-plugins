{
  "name": "@insightflo/paperclip-research-workbench",
  "version": "0.1.0",
  "description": "Research Workbench plugin for Paperclip — provides agent tools for web search and evidence gathering via Vane headless backend",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js",
    "worker": "./dist/worker.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "pnpm build && node --test ./tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "file:../../.paperclip-sdk/plugin-sdk",
    "@paperclipai/shared": "file:../../.paperclip-sdk/shared"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.7.3"
  }
}
